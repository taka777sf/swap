# スワップ積立トラッカー（PWA）

## 更新内容（v12）
- バックアップ案内の下に「皮算用」カードを追加。現在の保有合計ロットを元に、7日後・30日後の想定合計額を表示します
- 1ロットあたりの日割り単価（円）はその場で編集可能（25円、26円など日々変わる分に対応）

## 過去の更新内容（v11）
- 「✓ 8.4ロット」のようなチェックボタンから「ロット」の文字を削除し、数字のみ表示に統一

## 過去の更新内容（v10）
- 積み増し入力欄のプレースホルダーから「ロット」の文字を削除（記号「+」のみに）
- 確定済みロットボタンの色を緑から、未確定時と同じ黄色系に変更

## 過去の更新内容（v9）
- カレンダーのゲージ（進捗バー）と繰越金額の表示を廃止し、文字を大きくしました
- 閾値到達で自動計算されたロットは「☐ 0.1ロット」のようなチェックボタンで表示され、実際にLIGHT FX等で購入したらタップして確定（✓）。確定するまでは「保有合計」に反映されません（未確定分は今月獲得の欄に別途表示）
- 🛒ボタン（ポップアップ入力）を廃止し、代わりにロット数を直接入力できる欄をマスの中に常時表示。入力した数字はその日付にそのまま残ります
- 過去分のロットは全て「確定済み」として自動移行されるので、これまでの保有合計が急に0になることはありません

## 過去の更新内容（v8）
- 各日のマスに小さな「🛒」ボタンを追加。タップすると、その日にまとめて購入したロット数を入力できます（画面が細かくなるのを避けるため、通常時は目立たない小さいアイコンのみ表示）
- 保有合計に、初期保有＋自動加算＋まとめ購入分をすべて反映
- 項目が2つ以上ある場合、「全項目 保有合計」を単位ラベルごとにまとめて表示（例：106.4ロット / 12個）

## 過去の更新内容（v7）
- 「⇩保存/⇧復元」のバックアップ案内と、「記録開始前の繰越残高」欄を画面の一番下に移動しました
- 項目設定に「初期保有数（任意）」を追加：すでに保有しているロット数を入力しておくと、獲得ロットに加算した「合計保有数」を累計欄に表示します
- 閾値到達時のロット自動加算はこれまで通り自動で反映されます

## 過去の更新内容（v6）
- 週計に「全項目合計」も併記するようにしました（項目が2つ以上の場合のみ）

## 過去の更新内容（v5）
- カレンダーを月〜金＋週計の6列に変更し、iPhoneの画面幅に横スクロールなしで収まるようにしました（土日は非表示）
- ※土日にすでに入力していたデータがあっても削除はされません。計算・繰越・週計には引き続き反映されますが、表示上は非表示になります

## 過去の更新内容（v4）
- 項目を分けても、各日のマスに「全項目合計」を小さく併記するようにしました
- 月間スタッツに「今月合計（全項目）」を追加しました（項目が2つ以上の場合のみ表示）
- アプリ名を「スワップ積立トラッカー」に変更しました

## 過去の更新内容（v3）
- 端末のストレージ永続化をリクエストするようにしました（`navigator.storage.persist()`）。
  ヘッダーに「🛡 永続保存 有効」と出ればブラウザ側にも消去されにくいよう要求済みです
  （※Safari側の対応状況により反映されない場合もあります。100%消えない保証にはなりません）
- 最後にバックアップを書き出してから14日以上経つと、上部に控えめなリマインダーが出るようにしました
- アイコン等のファイル構成を「iconsフォルダなし・全部ルート直下」に統一しました
  （iPhoneのアップロード画面ではフォルダを作れないため、今後はこのままアップロードするだけでOKです）

## GitHub Pagesへの公開・更新手順

1. リポジトリのCodeタブを開く
2. 「Add file → Upload files」（または既存ファイルを開いて中身を書き換え）で、
   このフォルダの中身（index.html, style.css, app.js, manifest.json, service-worker.js,
   icon-192.png, icon-512.png, apple-touch-icon.png）を**すべてルート直下に**アップロード
   - 同じファイル名なら上書きされます。フォルダ分けは不要です
3. Settings → Pages で Branch=main / root になっていることを確認（初回のみ）
4. 数分待って `https://taka777sf.github.io/swap/` にアクセス

## iPhoneでアプリ化する

1. iPhoneのSafariで上記URLを開く
2. 共有ボタン→「ホーム画面に追加」
3. 既にアプリを追加済みの場合は、一度アプリを開いて数秒待つ（Service Workerが自動更新）

## データとバックアップについて

- 入力データはこの端末（iPhone）のローカルストレージに保存されます。他の端末とは自動同期されません
- 永続化リクエストにより消去されにくくなっていますが、100%の保証はありません
- ヘッダーの「⇩ 保存」で定期的にJSONバックアップを書き出し、iCloud DriveやPC等に保管することを推奨します
- 「⇧ 復元」で直近のバックアップファイルを読み込めば元に戻せます
- 複数端末での同期や、友達とのデータ共有が必要になったら、サーバー保存（VPS等）への切り替えも可能です


